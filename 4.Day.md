## 第三天

```js
// x  删除当前光标所在字符
// X  删除当前光标前的一个字符
// s  删除当前光标所在的字符并进入insert模式
// S  删除当前光标所在行并进入insert模式
// r  替换当前光标所在的字符
// R  替换多个字符，会按当前光标位置往后依次替换

// u  撤回，撤回的一次内容是从进入inset到回到normal模式之间修改的内容，而如果使用了方向键是，会当成单独的可撤回内容
// control + r 重做 


const a = 1
const b = 3


```